spring:
  application:
    name: gateway-metamorphic
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

server:
  port: 8180

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/